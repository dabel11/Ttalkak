# ── 서버 런타임 (app/) — Docker 이미지에 설치되는 것 ──
# 오프라인 적재/크롤링 의존성은 requirements-ingestion.txt 로 분리
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sentence-transformers>=3.0.0
google-genai>=1.0.0
groq>=0.11.0
pydantic>=2.0.0
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
PyMySQL>=1.1.0
numpy>=1.24.0
rank-bm25>=0.2.2      # use_hybrid opt-in 용 (지연 import)
kiwipiepy>=0.18.0     # BM25 한국어 형태소 토큰화 (지연 import)
