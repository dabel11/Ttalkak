(function attachAuthEvents(global) { "use strict"; function bindAuthControlEvents(root, controller) { root.querySelectorAll("[data-toggle-password]").forEach((button) => button.addEventListener("click", () => controller.togglePassword(button))); root.querySelectorAll("[data-google-auth]").forEach((button) => button.addEventListener("click", () => controller.googleLogin(button))); root.querySelectorAll("[data-check-duplicate]").forEach((button) => button.addEventListener("click", () => { const input = button.closest(".auth-check-row")?.querySelector("input"); controller.checkDuplicate(button.dataset.checkDuplicate, input); })); }
  function bindAuthFormEvents(root, controller, state) { const form = root.querySelector("[data-auth-form]"); if (!form) return; form.addEventListener("input", (event) => { const input = event.target; if (!(input instanceof global.HTMLInputElement)) return; controller.clearError(); if (input.name === "userId") { controller.normalizeUserId(input); controller.updateUserIdWarning(input); } if (state.authView === "signup") { controller.saveDraft(form); if (["nickname", "userId"].includes(input.name) && state.authDuplicateChecks[input.name] !== input.value.trim()) delete state.authDuplicateChecks[input.name]; } }); form.querySelectorAll("input[type='password']").forEach((input) => { input.addEventListener("keydown", (event) => controller.updateCapsLock(input, event)); input.addEventListener("keyup", (event) => controller.updateCapsLock(input, event)); input.addEventListener("blur", () => controller.updateCapsLock(input, null)); }); form.addEventListener("submit", (event) => { event.preventDefault(); controller.submit(form); }); }
  const api = Object.freeze({ bindAuthControlEvents, bindAuthFormEvents }); if (typeof module !== "undefined" && module.exports) module.exports = api; global.TtalkakAuthEvents = api; })(typeof window !== "undefined" ? window : globalThis);
