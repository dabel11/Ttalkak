(function attachAuthValidation(global) { "use strict"; const isValidEmail = (value) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(value || "").trim()); function getUserIdValidationMessage(value) { const id = String(value || "").trim(); if (!id) return ""; if (/[ㄱ-ㅎㅏ-ㅣ가-힣]/.test(id)) return "아이디에는 한글을 사용할 수 없습니다."; if (!/^[a-z0-9_-]+$/.test(id)) return "아이디는 영문 소문자, 숫자, _, -만 사용할 수 있습니다."; return ""; } const api = Object.freeze({ getUserIdValidationMessage, isValidEmail }); if (typeof module !== "undefined" && module.exports) module.exports = api; global.TtalkakAuthValidation = api; })(typeof window !== "undefined" ? window : globalThis);
